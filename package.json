{
  "name": "smart-tourist-safety",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w mobile\" \"npm run dev -w admin\" \"cd apps/ai && ./venv/bin/uvicorn app.main:app --port 8000 --reload\"",
    "start": "npm run dev",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5"
  }
}
